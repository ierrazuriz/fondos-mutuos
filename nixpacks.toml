[phases.setup]
nixPkgs = ["nodejs_20", "python312", "python312Packages.pip"]

[phases.install]
cmds = [
  "npm install",
  "pip install google-auth google-auth-oauthlib google-api-python-client pdfplumber"
]

[start]
cmd = "node index.js"
